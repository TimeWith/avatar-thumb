{
  "name": "@time-with/avatar-thumb",
  "version": "1.1.0",
  "description": "Avatar thumbnail display with text-based fallback",
  "main": "dist/index.js",
  "license": "MIT",
  "author": "Antonio Brandao <antoniobrandaodesign@gmail.com>",
  "scripts": {
    "prepack": "yarn && babel src --out-dir dist --source-maps && rm -rf node_modules",
    "postpack": "rm -rf node_modules"
  },
  "peerDependencies": {
    "@emotion/styled": "^10.0.7"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1"
  },
  "babel": {
    "presets": [
      "env",
      "react"
    ]
  }
}
